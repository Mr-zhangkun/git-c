#include <stdio.h>
int main()
{
    int a = -1;
    if(a > 0)
    {
        printf("a > 0\n");
    }
    if (a < 0)
    {
        printf("a < 0\n");
    }
    if(a == 0)
    {
        printf("a =%d\n", a);
    }
    printf("a = %d",a);
    return 0;
}
